diff --git a/dgl/Window.hpp b/dgl/Window.hpp
index 64d0f56..248427d 100644
--- a/dgl/Window.hpp
+++ b/dgl/Window.hpp
@@ -509,6 +509,9 @@ protected:
     */
     virtual uint32_t onClipboardDataOffer();
 
+    /** Called after accepted clipboard or file-drop data becomes available. */
+    virtual void onClipboardData(const char* mimeType, const void* data, size_t dataSize);
+
    /**
       A function called when the window is attempted to be closed.
       Returning true closes the window, which is the default behaviour.
diff --git a/dgl/src/Window.cpp b/dgl/src/Window.cpp
index 6f7562e..e6acad8 100644
--- a/dgl/src/Window.cpp
+++ b/dgl/src/Window.cpp
@@ -574,6 +574,10 @@ uint32_t Window::onClipboardDataOffer()
     return 0;
 }
 
+void Window::onClipboardData(const char*, const void*, size_t)
+{
+}
+
 bool Window::onClose()
 {
     return true;
diff --git a/dgl/src/WindowPrivateData.cpp b/dgl/src/WindowPrivateData.cpp
index d7a3c93..081c766 100644
--- a/dgl/src/WindowPrivateData.cpp
+++ b/dgl/src/WindowPrivateData.cpp
@@ -926,6 +926,15 @@ void Window::PrivateData::onClipboardData(const uint32_t typeId)
     if (clipboardTypeId != typeId)
         clipboardTypeId = 0;
 
+    if (clipboardTypeId != 0)
+    {
+        size_t dataSize = 0;
+        const void* const data = puglGetClipboard(view, clipboardTypeId - 1, &dataSize);
+        const char* const type = puglGetClipboardType(view, clipboardTypeId - 1);
+        if (data != nullptr && dataSize != 0 && type != nullptr)
+            self->onClipboardData(type, data, dataSize);
+    }
+
     waitingForClipboardData = false;
 }
 
diff --git a/distrho/DistrhoUI.hpp b/distrho/DistrhoUI.hpp
index 7741840..efa0493 100644
--- a/distrho/DistrhoUI.hpp
+++ b/distrho/DistrhoUI.hpp
@@ -283,6 +283,9 @@ protected:
     */
     virtual uint32_t uiClipboardDataOffer();
 
+    /** Receives accepted clipboard data, including text/uri-list file drops. */
+    virtual void uiClipboardData(const char* mimeType, const void* data, size_t dataSize);
+
    /**
       Windows focus function, called when the window gains or loses the keyboard focus.
       This function is for plugin UIs to be able to override Window::onFocus(bool, CrossingMode).
diff --git a/distrho/src/DistrhoUI.cpp b/distrho/src/DistrhoUI.cpp
index 0241b59..50fa172 100644
--- a/distrho/src/DistrhoUI.cpp
+++ b/distrho/src/DistrhoUI.cpp
@@ -556,6 +556,10 @@ uint32_t UI::uiClipboardDataOffer()
     return 0;
 }
 
+void UI::uiClipboardData(const char*, const void*, size_t)
+{
+}
+
 void UI::uiFocus(bool, DGL_NAMESPACE::CrossingMode)
 {
 }
diff --git a/distrho/src/DistrhoUIPrivateData.hpp b/distrho/src/DistrhoUIPrivateData.hpp
index 108e76b..40e0b15 100644
--- a/distrho/src/DistrhoUIPrivateData.hpp
+++ b/distrho/src/DistrhoUIPrivateData.hpp
@@ -203,6 +203,13 @@ protected:
         return ui->uiClipboardDataOffer();
     }
 
+    void onClipboardData(const char* const mimeType, const void* const data, const size_t dataSize) override
+    {
+        DISTRHO_SAFE_ASSERT_RETURN(ui != nullptr,);
+        if (!initializing)
+            ui->uiClipboardData(mimeType, data, dataSize);
+    }
+
     void onFocus(const bool focus, const DGL_NAMESPACE::CrossingMode mode) override
     {
         DISTRHO_SAFE_ASSERT_RETURN(ui != nullptr,);
